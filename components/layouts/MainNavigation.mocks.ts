import { IMainNavigation } from './MainNavigation';

const base: IMainNavigation = {
  home: 'Home',
  search: 'Search',
  browse: 'Browse',
  addlisting: 'Add Listing',
};

export const mockMainNavigationProps = {
  base,
};
